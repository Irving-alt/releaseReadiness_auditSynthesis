# Contributing

## Baseline Policy
- Files under `data/` are baseline inputs.
- Do NOT modify `data/` unless the task explicitly asks you to.

## Output Policy
- You may add files under `dashboard/` and `.github/workflows/`.
